---
id: RD-141
title: E2E Harness Scaffold And Selector Audit
epic: E
lane: [e2e]
priority: P1-e2e
status: todo
deps: []
rules: [global, e2e]
owns:
  - apps/e2e/**
  - package.json
  - .gitignore
---

# RD-141 E2E Harness Scaffold And Selector Audit

## Goal
Stand up `apps/e2e` with a three-project Playwright config and one passing smoke test, and produce the list of selectors the flow tickets will need.

## Implementation
- `pnpm --filter @casium/e2e add -D @playwright/test` (pin to the `playwright@1.61.1` already in the lockfile — a version skew changes the `~/.cache/ms-playwright/chromium-*` directory the opencode MCP config pins, see `docs/browser-testing.md`). `playwright.config.ts`: `testDir: 'specs'`, `fullyParallel: true`, `forbidOnly: !!process.env.CI`, `retries: process.env.CI ? 1 : 0`, `reporter: [['list'], ['html', { open: 'never' }]]`, `use: { baseURL: 'http://localhost:3000', trace: 'on-first-retry', video: 'retain-on-failure' }`.
- Three projects filtered by `testMatch`/`grep`: `stubbed`, `live-services`, `wallet` (the last two defined here but empty until RD-148/RD-150; the `wallet` project additionally sets `workers: 1`, `trace: 'off'`, `video: 'off'` — see the Rules table on profile hygiene). `webServer` for the web app only in this ticket: `command: 'pnpm --filter @casium/web build && pnpm --filter @casium/web start'`, `url: 'http://localhost:3000'`, `reuseExistingServer: !process.env.CI`, `timeout: 180_000`.
- Add root scripts `test:e2e` → `pnpm --filter @casium/e2e e2e:stubbed`. Then walk `/`, `/renter`, `/provider`, `/agent`, `/landlord` in the browser and write `apps/e2e/README.md` § *Selector audit*: for each assertion the flow tickets need, the selector that works today, or `NEEDS TESTID` with the component and line.

## Done when
- [ ] `pnpm --filter @casium/e2e e2e:stubbed` builds the web app, starts it, and passes one smoke test asserting `/` renders `Casium` and three role links.
- [ ] `pnpm -r --if-present test` still runs exactly the pre-existing suites — no browser launches.
- [ ] `pnpm -r --if-present build` unaffected.
- [ ] The selector audit exists and names every `NEEDS TESTID` case.

## Verify
- **Tests.** The smoke spec is the test.
- **Evidence to capture.** Paste the Playwright summary line and the output of `pnpm -r --if-present test` showing unchanged suite count.

## Notes
- **Failure fallback.** If the pinned Chromium is missing, `pnpm exec playwright install chromium` into the default cache — do not change the MCP-pinned path in `opencode.json`.
- **Parallel safety.** Owns `apps/e2e` exclusively. Do it first, alone.
- **Integration.** None (infrastructure).
- **Demo impact.** None directly; everything else depends on it.
