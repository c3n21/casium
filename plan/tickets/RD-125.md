---
id: RD-125
title: Storage Mode Selection And Honest Labeling
epic: W
lane: [walrus, frontend]
priority: P1-completion
status: done
deps: [RD-122]
rules: [global, privacy, frontend]
owns:
  - packages/walrus/src/index.ts
  - apps/web/src/components/PacketBuilder.tsx
  - apps/agent/src/index.ts
  - apps/provider-api/src/app.ts
  - /health
  - README.md
---

# RD-125 Storage Mode Selection And Honest Labeling

## Goal
One switch chooses the storage backend everywhere, and the UI never misrepresents which is active.

## Implementation
Single `WALRUS_MODE` / `NEXT_PUBLIC_WALRUS_MODE` of `mock | http | cli`, resolved in one factory in `packages/walrus` and consumed by web, agent, and provider. Mock blob IDs keep the `mock:` prefix. Every surface that displays a blob ID — `PacketBuilder`, the provider inbox, the landlord panel, the README integration table — must render the active mode from the value, not from a prop default. `PacketBuilder`'s current `walrusMode = "mock"` prop default is exactly the failure mode to remove: it can display `mock` while something else is configured, or vice versa.

## Done when
- [ ] Changing one env var moves every surface between mock and live with no code edit.
- [ ] A blob ID starting with `mock:` can never render as live, and a real blob ID can never render as mock.
- [ ] `/health` reports the active storage mode.

## Verify
- **Tests.** Factory unit tests for each mode; a component test asserting the label follows the blob ID prefix.
- **Evidence to capture.** Screenshots of the same page in both modes.
- **Browser.** Required — record the env var value alongside each screenshot, since the whole ticket is about the label matching reality. See `docs/browser-testing.md`.

## Notes
- **Failure fallback.** None; this is small and it is the integration-integrity guardrail.
- **Parallel safety.** Touches several apps shallowly; land it in the same wave as RD-122.
- **Integration.** Walrus.
- **Demo impact.** Protects against the worst possible outcome — accidentally overclaiming a live integration.
