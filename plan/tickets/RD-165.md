---
id: RD-165
title: Docs And Honest-Claim Update
epic: I
lane: [docs]
priority: P2-identity
status: done
deps: [RD-163, RD-164]
rules: [global]
owns:
  - docs/world-agentkit.md
  - docs/provider-api.md
  - docs/demo-script.md
  - README.md
---

# RD-165 Docs And Honest-Claim Update

## Goal
State exactly what the identity binding is, where each half is enforced, and what remains unenforced — so the claim survives a reviewer's follow-up question.

## Implementation
Add an *Agent Identity Binding* section to `docs/world-agentkit.md`: the two-address table from the *Source Of Truth* section of `plan/epics/I.md`, which layer enforces which half (Move enforces `agent_sui` via `sender`; the provider enforces `agent_evm` via AgentBook + RD-164; **nothing enforces `agent_evm` on-chain, by design** — link the *What This Epic Does Not Do* table in `plan/epics/I.md`). Update the error-code table in `docs/provider-api.md` with the two 403s and their new meaning. Update `README.md`'s integration rows only where RD-164 changed what is true. Add one line to `docs/demo-script.md` Step 4 noting the renter no longer types the agent's addresses — that is a visible on-stage difference. If Epic E is live, coordinate the `AGENTS.md` edit with RD-152 rather than both writing it.

## Done when
- [ ] Every claim in the new section maps to a file and line that a reader can check.
- [ ] No sentence says "verified" about a value the code only records.
- [ ] The demo script matches what the form actually does.

## Verify
- **Tests.** None (docs).
- **Evidence to capture.** Paste the new `docs/world-agentkit.md` section and the diff summary for the other three files.

## Notes
- **Failure fallback.** If RD-164 lands as `BLOCKED`, still ship the docs — describing `agent_evm` as *recorded, not enforced*, with the reason. An accurate limitation is publishable; an overclaim is not.
- **Parallel safety.** Owns the docs files; no overlap with the code tickets.
- **Integration.** World.
- **Demo impact.** Medium — this is the answer to "how do you know the agent is the one the renter authorized?"
