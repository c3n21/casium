---
id: RD-161
title: Agent Identity Endpoint
epic: I
lane: [agent]
priority: P1-identity
status: done
deps: []
rules: [global, agent]
owns:
  - apps/agent/src/server.ts
  - apps/agent/src/types.ts
  - README.md
---

# RD-161 Agent Identity Endpoint

## Goal
Make the agent service the source of truth for its own identity pair, so no human has to transcribe either address.

## Implementation
In `apps/agent/src/server.ts`, add `agentEvmAddress` to the existing `GET /health` payload — the value is already read as `process.env.AGENTKIT_DEMO_AGENT_EVM_ADDRESS` in `readDemoAgentKitHeaders()` (line 38), and in live mode it comes from `AGENTKIT_HEADER`'s signer. Return `null` when unknown rather than an empty string, so a consumer can distinguish "not configured" from "configured as blank". Then add `GET /identity` returning `{ agentSuiAddress, agentEvmAddress, agentkitMode, packageId }` — a dedicated endpoint so the mandate form is not coupled to the shape of a liveness probe, and so a future agent registry can serve the same shape. `packageId` comes from `@casium/contracts-config`; it lets the form warn when the agent targets a different package than the browser. CORS is already open (`app.use("*", cors(...))`, line 61). Do not add authentication — this endpoint publishes only public addresses. Keep `/health`'s existing keys untouched: `apps/web/app/agent/page.tsx:323-326` reads `agentSuiAddress` and `agentkitMode`, and RD-144's stubs mirror the shape.

## Done when
- [ ] `GET /health` returns its current keys **plus** `agentEvmAddress`.
- [ ] `GET /identity` returns all four fields.
- [ ] With `AGENTKIT_DEMO_AGENT_EVM_ADDRESS` unset, `agentEvmAddress` is `null` and neither endpoint throws.
- [ ] `pnpm --filter @casium/agent test` and `build` pass.
- [ ] The `/agent` page still renders "Agent online" unchanged.

## Verify
- **Tests.** Unit tests for both handlers: configured pair, unset EVM address, and mock-vs-live `agentkitMode`.
- **Evidence to capture.** Paste both `curl` responses (mock mode), and a screenshot or text confirmation that `/agent` still shows "Agent online".

## Notes
- **Failure fallback.** If live mode cannot expose the EVM address without parsing `AGENTKIT_HEADER`, return `null` in live mode and record the limitation in the ticket — RD-163 must handle `null` regardless.
- **Parallel safety.** Owns `apps/agent/src/server.ts`. Coordinate if any Epic C agent work is live.
- **Integration.** World (agent identity surface).
- **Demo impact.** None directly; unblocks RD-163.
