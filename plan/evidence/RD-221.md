# RD-221 — evidence

Status: **DONE**

2026-07-31. **The ticket understated the problem and its `owns:` could not satisfy its own
"Done when".** It named two hand-typed municipality tables; an audit found four, and they disagreed
about code 6 four different ways:

| Source | code 6 |
|---|---|
| `packages/shared/src/constants.ts` (canonical) | `"Porto"` |
| `MandateForm.tsx:17` | absent — five entries only |
| `renter/page.tsx:47` | `"Porto (ineligible)"` |
| `ListingForm.tsx:15` | `"Porto (ineligible demo)"` |
| `provider/page.tsx:17` | `"Porto (ineligible demo)"` |

Two of the four files sat outside the declared `owns:`, so "no municipality literal remains under
`apps/web`" was unreachable as written. The ticket's `owns:` and Context were corrected first
(user-authorized), then the work was done across all four.

## What landed

All four tables deleted; every file now reads `MUNICIPALITIES` / `MUNICIPALITY_LABELS` from
`@casium/shared`. `MandateForm` exports `MANDATE_MUNICIPALITIES`, derived by filtering
`PORTO_INELIGIBLE_DEMO` out of the shared map, so code 6 remains unselectable as a mandate target by
construction rather than by omission. It stays visible in `ListingForm`'s select, the provider
listings table and the renter labels.

The `"(ineligible demo)"` wording is derived at the provider render site.
`apps/e2e/specs/provider-dashboard.spec.ts:24` asserts that exact string and `apps/e2e` was out of
scope, so the rendered text is byte-identical while the base label comes from shared. The renter page
now shows the canonical `"Porto"` — a deliberate, visible change, chosen for consistency because no
spec pins renter-page wording.

## Verification

- `pnpm --filter @casium/web test` → 2 files, 11 tests passed, exit 0.
- `pnpm --filter @casium/web typecheck` → exit 0. `build` → exit 0.
- `pnpm test:e2e` → **25 passed, 0 failed**, exit 0. Green before and after.
- `node scripts/check-object-ids.mjs` → still exactly 3 pre-existing findings; none introduced.
- Screenshots `after-221` are byte-identical to `after-183` on all five routes, `/provider` included.
  The renter label change is not visible there: the screenshot flow does not render listing rows.

## Weaker than the ticket asked for

The ticket wanted `MandateForm.test.ts` to assert the options come from the shared constant. The test
cannot import `MandateForm.tsx`: it pulls in `@/` alias imports and `apps/web/vitest.config.ts`
defines no path alias (pre-existing — `packet.test.ts`, the only other test, uses relative imports
only, and the vitest config was out of scope). The added tests therefore assert the derivation
contract against the shared constants directly, plus read `MandateForm.tsx`'s source text to confirm
it imports from `@casium/shared`, contains no label literals, and references
`PORTO_INELIGIBLE_DEMO`. That is a real check of the invariant but not a render-level one. Adding a
path alias to `apps/web/vitest.config.ts` would let this become a proper component test.
