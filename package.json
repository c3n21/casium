{
  "name": "rentdelegate",
  "version": "0.0.0",
  "private": true,
  "description": "RentDelegate monorepo for scoped rental application agents on Sui and World.",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test"
  }
}
