services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: rentdelegate
      POSTGRES_PASSWORD: rentdelegate
      POSTGRES_DB: rentdelegate
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
