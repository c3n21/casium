services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: casium
      POSTGRES_PASSWORD: casium
      POSTGRES_DB: casium
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
