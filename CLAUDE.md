# Casium

**Read `AGENTS.md` now — it holds the environment constraints and hard rules for this repo.**

Then, if you are picking up a ticket, read `plan/START.md`.

<!--
Deliberately a pointer, not a copy. Every fact lives in AGENTS.md so the two files cannot
disagree, and so a harness that loads both does not pay for the same content twice.
Do not add content here.
-->
